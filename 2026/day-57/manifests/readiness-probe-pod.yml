apiVersion: v1
kind: Pod
metadata:
  name: readiness-probe-pod
  labels:
    app: nginx
spec:
  containers:
    - name: readiness-probe-ctr
      image: nginx
      readinessProbe:
        httpGet:
          path: /
          port: 80
        periodSeconds: 5