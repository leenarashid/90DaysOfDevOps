apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-ecs-app.fullname" . }}
  labels:
    {{- include "flask-ecs-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort}}
      protocol: TCP
      name: http
  selector:
    {{- include "flask-ecs-app.selectorLabels" . | nindent 4 }}
